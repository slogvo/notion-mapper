import { ColumnBlock } from "../types/column.types";

export const Column = ({ block }: { block: ColumnBlock }) => (
  <div className="flex-1">
    {/* Nội dung con sẽ được render trong blockMapper */}
  </div>
);
